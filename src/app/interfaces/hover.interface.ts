/**
 * Interface representing the hover state of an element.
 * It defines the text values for neutral and hovered states and a flag indicating whether the element is currently hovered.
 */
export interface Hover {
    neutral: string;
    hovered: string;
    isHovered: boolean;
}